swagger: "2.0"
info:
  title: Actions API
  description: The Actions API.
  version: 1.0.0
host: www.site24x7.com.
basePath: /
schemes:
- http
produces:
- application/json
consumes:
- application/json
paths:
  n    /api/{version}/actions:
    n    get:
      summary: Get Actions (Alerts) List
      description: '    Returns a list of actions (alerts) that have been generated
        for your account.'
      operationId: get-actions-alerts-list
      parameters:
      - in: query
        name: checkids
        description: Comma-separated list of check identifiers. Limit results to actions
          generated from these checks.
        type: <td>string</td>
      - in: query
        name: contactids
        description: Comma-separated list of contact identifiers. Limit results to
          actions sent to these contacts.
        type: <td>string</td>
      - in: query
        name: from
        description: Only include actions generated later than this timestamp. Format
          is UNIX time.
        type: <td>integer</td>
      - in: query
        name: limit
        description: Limits the number of returned results to the specified quantity.
        type: <td>integer (max 300)</td>
      - in: query
        name: offset
        description: Offset for listing.
        type: <td>integer</td>
      - in: query
        name: status
        description: Comma-separated list of statuses. Limit results to actions with
          these statuses.
        type: <td>string (sent, delivered, error, not_deliv
      - in: query
        name: to
        description: Only include actions generated prior to this timestamp. Format
          is UNIX time.
        type: <td>integer</td>
      - in: query
        name: via
        description: Comma-separated list of via mediums. Limit results to actions
          with these mediums.
        type: <td>string (email, sms, twitter, iphone, andr
      responses:
        200:
          description: OK
      tags:
      - Actions
  /actions:
    get:
      summary: List Actions
      description: List of all Actions.
      operationId: list-actions
      parameters:
      - in: path
        name: maintenance_idn        n        n            stringn            Unique
          ID generated by the server. This can be used as an identifier.n        n                n    n        n        display_namen        n        n            stringn            Displa
      responses:
        200:
          description: OK
      tags:
      - Actions
    post:
      summary: Create Action
      description: Create a new Action.
      operationId: create-action
      parameters:
      - in: path
        name: action_namen        n        n            requiredn            Display
          name for the Action.n        n    n    n        n        action_urln        n        n            requiredn            URL
          to be invoked for action execution.
      responses:
        200:
          description: OK
      tags:
      - Actions
  /actions/{action_id}:
    put:
      summary: Update Action
      description: Update an existing Action.
      operationId: update-action
      parameters:
      - in: path
        name: action_namen        n        n            requiredn            Display
          name for the Action.n        n    n    n        n        action_urln        n        n            requiredn            URL
          to be invoked for action execution.
      responses:
        200:
          description: OK
      tags:
      - Actions
  /v1/actions:
    get:
      summary: Get all available action types
      description: Gets all known action types
      operationId: get-all-available-action-types
      responses:
        200:
          description: OK
      tags:
      - Actions
  /v1/actions/:action:
    get:
      summary: Get action
      description: Gets a single action type
      operationId: get-action
      parameters:
      - in: query
        name: actionnName of plugin
        type: string
      responses:
        200:
          description: OK
      tags:
      - Actions
  /v1/actions/installed:
    get:
      summary: Get all installed actions
      description: Gets all actions that are installed for the project
      operationId: get-all-installed-actions
      responses:
        200:
          description: OK
      tags:
      - Actions
    put:
      summary: Install action
      description: Installs an action
      operationId: install-action
      responses:
        200:
          description: OK
      tags:
      - Actions
  /v1/actions/installed/:actionId:
    delete:
      summary: Uninstall action
      description: Uninstalls an action and remove it from the associated alarms(s).
      operationId: uninstall-action
      responses:
        200:
          description: OK
      tags:
      - Actions
    get:
      summary: Get details of an installed action
      description: Gets a single action definition
      operationId: get-details-of-an-installed-action
      parameters:
      - in: query
        name: actionIdnName of plugin
        type: string
      responses:
        200:
          description: OK
      tags:
      - Actions
  /v1/actions/installed/:actionId/alarms:
    get:
      summary: Get all alarms using an action
      description: Get alarms that are using this action installed for the project
      operationId: get-all-alarms-using-an-action
      responses:
        200:
          description: OK
      tags:
      - Actions
definitions: []