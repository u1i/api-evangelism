swagger: "2.0"
info:
  title: User Group API
  description: The User Group API.
  version: 1.0.0
host: www.site24x7.com.
schemes:
- http
produces:
- application/json
consumes:
- application/json
paths:
  /user_groups:
    get:
      summary: List of all User Groups
      description: List of all User Groups.
      operationId: list-of-all-user-groups
      parameters:
      - in: path
        name: profile_idn        n        n            stringn            Unique ID
          generated by the server. This can be used as an identifier.n        n                n    n        n        profile_namen        n        n            stringn            Display
          na
      responses:
        200:
          description: OK
      tags:
      - ""
    post:
      summary: Create a new user group
      description: Create a new user group.
      operationId: create-a-new-user-group
      parameters:
      - in: path
        name: display_namen        n        n            requiredn            Display
          name for the user group.n        n    n    n        n        usersn        n        n            requiredn            Users
          to be associated for the group.
      responses:
        200:
          description: OK
      tags:
      - ""
  /user_groups/{user_group_id}:
    put:
      summary: Update an user group details
      description: Update an existing User Group Information.
      operationId: update-an-user-group-details
      parameters:
      - in: path
        name: display_namen        n        n            requiredn            Display
          name for the user group.n        n    n    n        n        usersn        n        n            requiredn            Users
          to be associated for the group.
      responses:
        200:
          description: OK
      tags:
      - ""
definitions: []